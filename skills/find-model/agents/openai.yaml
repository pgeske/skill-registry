interface:
  display_name: "Find Model"
  short_description: "Find and add supported OpenClaw models"
  default_prompt: "Find whether a model is supported by OpenClaw and add it to config without changing defaults unless asked."
